# Environment variables
# Copy this file to apps/web/.env.local for local overrides. Nothing here is secret.

# Canonical origin used for metadata, sitemap and robots (defaults to https://miloscvetkovic.dev).
NEXT_PUBLIC_SITE_URL=https://miloscvetkovic.dev
