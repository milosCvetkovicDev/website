command -v pnpm >/dev/null 2>&1 || { export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"; }
command -v pnpm >/dev/null 2>&1 || { echo "husky: pnpm is not on PATH (see README > Development)" >&2; exit 1; }
pnpm exec commitlint --edit "$1"
